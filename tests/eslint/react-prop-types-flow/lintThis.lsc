import React from 'react'

export statelessComponent1(props: { data: string }) ->
  <div>
    {props.data}
  </div>

export statelessComponent2({ data }: { data: string }) ->
  <div>
    {data}
  </div>

export statelessComponent3({data}) ->
  <div>
    {data}
  </div>
